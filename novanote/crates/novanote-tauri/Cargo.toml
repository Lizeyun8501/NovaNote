[package]
name = "novanote-tauri"
version = "0.1.0"
edition = "2021"

[lib]
name = "novanote_tauri"
crate-type = ["lib"]

[dependencies]
novanote-core = { path = "../novanote-core" }
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"